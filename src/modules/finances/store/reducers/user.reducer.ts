import { User } from '../../../authentication/models/user.model';import * as fromUsers from '../actions/user.action';export interface UserState {  user: User;  loaded: boolean;  loading: boolean;}export const initialUserState: UserState = {  user: {    email: '',    expenses: [],    incomes: [],    isAdmin: false,    _id: ''  },  loaded: false,  loading: false};export function userReducer(state = initialUserState, action: fromUsers.UserAction): UserState {  switch (action.type) {    case fromUsers.LOAD_USER: {      return { ...state, loading: true, loaded: false };    }    case fromUsers.LOAD_USER_SUCCESS: {      const user = action.payload;      return { user, loaded: true, loading: false };    }    case fromUsers.LOAD_USER_FAIL: {      return { ...state, loading: false, loaded: false };    }  }}