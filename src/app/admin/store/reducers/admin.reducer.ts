import { FullUser } from '../../models/full-user.model';import * as fromAdmin from '../actions/admin.actions';export interface AdminState {  users: FullUser[];  loading: boolean;}const initialState: AdminState = {  users: [],  loading: false};export function adminReducer(state: AdminState = initialState, action: fromAdmin.AdminActions) {  switch (action.type) {    case fromAdmin.GET_USERS: return { ...state, loading: true };    case fromAdmin.GET_USERS_FAIL: return { ...state, loading: false };    case fromAdmin.GET_USERS_SUCCESS: return { users: action.payload, loading: false };    default: return state;  }}export const getAdminUsers = (state: AdminState) => state.users;export const getAdminLoading = (state: AdminState) => state.loading;