import { createFeatureSelector, createSelector } from '@ngrx/store';import { AdminState, getAdminLoading, getAdminUsers } from '../reducers';// selector zwracający główny stan całego feature'a - taka sama nazwa jak w rejestracji w feature module// {glownaNazwa: {authenticationState: { toCoWStanie }}}export const getAdminFeatureState = createFeatureSelector<AdminState>('adminFeature');export const adminSelectors = {  users: createSelector(getAdminFeatureState, getAdminUsers),  loading: createSelector(getAdminFeatureState, getAdminLoading),};