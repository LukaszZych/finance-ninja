import { User } from '../../../authentication/models/user.model';import * as fromUsers from '../actions/user.actions';// interfejs tylko dla tej części stanu w tym moduleexport interface UserState {  user: User;  loaded: boolean;  loading: boolean;}// inicjalny stan tylko dla tej części stanu w tym moduleexport const initialUserState: UserState = {  user: {    email: '',    expenses: [],    incomes: [],    isAdmin: false,    _id: ''  },  loaded: false,  loading: false};export function userReducer(state = initialUserState, action: fromUsers.UserActions): UserState {  switch (action.type) {    case fromUsers.LOAD_USER: {      return { ...state, loading: true, loaded: false };    }    case fromUsers.LOAD_USER_SUCCESS: {      const user = action.payload;      return { user, loaded: true, loading: false };    }    case fromUsers.LOAD_USER_FAIL: {      return { ...state, loading: false, loaded: false };    }    default: return state;  }}