import { createFeatureSelector, createSelector } from '@ngrx/store';import { AuthenticationState, getAuthIsAdmin, getAuthLoading, getAuthToken } from '../reducers/index';// selector zwracający główny stan całego feature'a - taka sama nazwa jak w rejestracji w feature module// {glownaNazwa: {authenticationState: { toCoWStanie }}}export const getAuthenticationFeatureState = createFeatureSelector<AuthenticationState>('authenticationFeature');export const authenticationSelectors = {  token: createSelector(getAuthenticationFeatureState, getAuthToken),  loading: createSelector(getAuthenticationFeatureState, getAuthLoading),  isAdmin: createSelector(getAuthenticationFeatureState, getAuthIsAdmin)};